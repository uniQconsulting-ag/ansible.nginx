---
# Role
role_include_files: # add for example requirements.yml
- install.yml
- config.yml
- selinux.yml

# Packages
nginx_packages_centos8:
- nginx-more
nginx_aeris_key: https://repo.aerisnetwork.com/pub/RPM-GPG-KEY-AERIS
nginx_aeris_repo: https://repo.aerisnetwork.com/pub/aeris-release-8.rpm


# Log
nginx_log_dir: /var/log/nginx
nginx_access_log: "{{ nginx_log_dir }}/access.log"
nginx_error_log: "{{ nginx_log_dir }}/error.log"

# Nginx
nginx_user: nginx
nginx_pid: /run/nginx.pid
nginx_php_socket_dir: /run/php
nginx_config_dir: /etc/nginx/conf.d

...
